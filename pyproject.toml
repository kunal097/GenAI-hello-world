[project]
name = "hello-langchain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3.25",
    "langchain-ollama>=0.3.2",
    "streamlit>=1.45.0",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
    "watchdog>=6.0.0",
]
